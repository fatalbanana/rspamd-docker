name: lametest

on:
  workflow_dispatch:

jobs:
  bump:
    runs-on: "ubuntu-22.04"
    permissions:
      contents: write
    steps:
      - name: Bump tag for no particular reason
        uses: ./.github/actions/bump_tag
